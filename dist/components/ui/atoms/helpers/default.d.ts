import { ClassesOptions } from '../interfaces';
export declare const generateClasses: ({ textAlign, width, verticalMargin, marginRight, marginLeft, vertical, horizontal, textSize, weight, textColor, textColorHover, bgColor, bgHoverColor, borderColor, shadow, className }: ClassesOptions) => string;
